#include <stdlib.h>
#include <stdio.h>

int run() {
	fwrite("Good... Wait what?\n", 0x1, 0x13, stdout);
	return system("/bin/sh");
}

int main(int ac, char **av) {
	char buffer[76];
	gets(buffer);
	return (0);
}
